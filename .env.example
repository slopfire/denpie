# Required for SQLx compile-time query validation.
DATABASE_URL="sqlite://dailytip.db"

# NOTE: LLM credentials (API key, base URL, model) are NOT configured here.
# They are managed through the admin dashboard at /admin and stored in settings.yaml.
# settings.yaml is intentionally excluded from version control.
